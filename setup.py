from setuptools import setup, find_packages

setup(
    name="mbt_ai_tools",
    version="0.1.0",
    description="Modernized MBT contradiction detection and recursive AGI toolkit",
    author="MBT",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=["numpy"],
)
